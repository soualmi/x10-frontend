services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: oracle-frontend
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    networks:
      - oracle-network

networks:
  oracle-network:
    driver: bridge
