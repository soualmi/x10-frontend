services:
  frontend:
    build: .
    ports:
      - "3001:3000"
    environment:
      - APIFY_TOKEN=${APIFY_TOKEN}
    networks:
      - oracle-network

networks:
  oracle-network:
    name: x10-frontend_oracle-network
