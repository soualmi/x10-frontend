# Oracle — Environment variables (minimum)

# Required: API token used to fetch e-commerce signals via Apify
APIFY_TOKEN=

# Optional: set explicit environment (Next.js / Node)
# NODE_ENV=production
